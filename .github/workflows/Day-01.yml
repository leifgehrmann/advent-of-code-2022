name: Day-01-Calorie-Counting

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/day_01**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: cargo build --verbose
      - name: Run tests
        run: cargo run -- 01
