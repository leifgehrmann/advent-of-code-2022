name: Day-02-???

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/day_02*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: cargo build --verbose
      - name: Run tests
        run: cargo run -- 02
